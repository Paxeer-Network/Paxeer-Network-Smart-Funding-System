/**
 * Default SDK configuration values
 */
export const DEFAULT_CONFIG = {
  BASE_URL: 'https://us-east-1.user-stats.sidiora.exchange',
  TIMEOUT: 30000,
  RETRY_ATTEMPTS: 3,
  RETRY_DELAY: 1000,
} as const;

/**
 * API version
 */
export const API_VERSION = '0.1.0';

/**
 * SDK version
 */
export const SDK_VERSION = '1.0.0';
